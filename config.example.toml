relays = [
  "wss://nostr.0cx.de",
]

display_name = "anon"
max_messages = 500

# Channels to auto-join on startup (kind-40 event IDs)
bookmarks = []

# Your Nostr profile (NIP-01 kind 0), published to relays on startup.
[profile]
name = "anon"
display_name = "Anonymous User"
about = ""
# picture = "https://example.com/avatar.png"
